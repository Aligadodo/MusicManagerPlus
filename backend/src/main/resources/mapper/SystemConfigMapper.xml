<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.filemanager.backend.mapper.SystemConfigMapper">

    <resultMap id="SystemConfigResultMap" type="com.filemanager.backend.entity.SystemConfigPO">
        <id property="configKey" column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="configType" column="config_type"/>
        <result property="description" column="description"/>
        <result property="category" column="category"/>
        <result property="isEncrypted" column="is_encrypted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.filemanager.backend.entity.SystemConfigPO">
        INSERT INTO system_config (
            config_key, config_value, config_type, description, category,
            is_encrypted, created_at, updated_at
        ) VALUES (
            #{configKey}, #{configValue}, #{configType}, #{description}, #{category},
            #{isEncrypted}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update" parameterType="com.filemanager.backend.entity.SystemConfigPO">
        UPDATE system_config
        <set>
            <if test="configValue != null">config_value = #{configValue},</if>
            <if test="configType != null">config_type = #{configType},</if>
            <if test="description != null">description = #{description},</if>
            <if test="category != null">category = #{category},</if>
            <if test="isEncrypted != null">is_encrypted = #{isEncrypted},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE config_key = #{configKey}
    </update>

    <delete id="deleteByConfigKey">
        DELETE FROM system_config WHERE config_key = #{configKey}
    </delete>

    <select id="selectByConfigKey" resultMap="SystemConfigResultMap">
        SELECT * FROM system_config WHERE config_key = #{configKey}
    </select>

    <select id="selectAll" resultMap="SystemConfigResultMap">
        SELECT * FROM system_config ORDER BY created_at DESC
    </select>

    <select id="selectByCategory" resultMap="SystemConfigResultMap">
        SELECT * FROM system_config
        WHERE category = #{category}
        ORDER BY created_at DESC
    </select>

    <select id="selectByPage" resultMap="SystemConfigResultMap">
        SELECT * FROM system_config
        <where>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
        </where>
        <choose>
            <when test="sortBy != null and sortBy != ''">
                ORDER BY ${sortBy} ${sortOrder}
            </when>
            <otherwise>
                ORDER BY created_at DESC
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByPage" resultType="int">
        SELECT COUNT(*) FROM system_config
        <where>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
        </where>
    </select>

</mapper>
