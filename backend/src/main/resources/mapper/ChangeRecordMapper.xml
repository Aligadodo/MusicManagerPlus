<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.filemanager.backend.mapper.ChangeRecordMapper">

    <resultMap id="ChangeRecordResultMap" type="com.filemanager.backend.entity.ChangeRecordPO">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="recordId" column="record_id"/>
        <result property="originalName" column="original_name"/>
        <result property="newName" column="new_name"/>
        <result property="filePath" column="file_path"/>
        <result property="newPath" column="new_path"/>
        <result property="operationType" column="operation_type"/>
        <result property="status" column="status"/>
        <result property="changed" column="changed"/>
        <result property="selected" column="selected"/>
        <result property="failReason" column="fail_reason"/>
        <result property="extraParams" column="extra_params"/>
        <result property="analyzeTime" column="analyze_time"/>
        <result property="executeTime" column="execute_time"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.filemanager.backend.entity.ChangeRecordPO">
        INSERT INTO change_record (
            task_id, record_id, original_name, new_name, file_path, new_path,
            operation_type, status, changed, selected, fail_reason, extra_params,
            analyze_time, execute_time, created_at
        ) VALUES (
            #{taskId}, #{recordId}, #{originalName}, #{newName}, #{filePath}, #{newPath},
            #{operationType}, #{status}, #{changed}, #{selected}, #{failReason}, #{extraParams},
            #{analyzeTime}, #{executeTime}, #{createdAt}
        )
    </insert>

    <update id="update" parameterType="com.filemanager.backend.entity.ChangeRecordPO">
        UPDATE change_record
        <set>
            <if test="newName != null">new_name = #{newName},</if>
            <if test="newPath != null">new_path = #{newPath},</if>
            <if test="operationType != null">operation_type = #{operationType},</if>
            <if test="status != null">status = #{status},</if>
            <if test="changed != null">changed = #{changed},</if>
            <if test="selected != null">selected = #{selected},</if>
            <if test="failReason != null">fail_reason = #{failReason},</if>
            <if test="extraParams != null">extra_params = #{extraParams},</if>
            <if test="analyzeTime != null">analyze_time = #{analyzeTime},</if>
            <if test="executeTime != null">execute_time = #{executeTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM change_record WHERE id = #{id}
    </delete>

    <delete id="deleteByTaskId">
        DELETE FROM change_record WHERE task_id = #{taskId}
    </delete>

    <select id="selectById" resultMap="ChangeRecordResultMap">
        SELECT * FROM change_record WHERE id = #{id}
    </select>

    <select id="selectByTaskId" resultMap="ChangeRecordResultMap">
        SELECT * FROM change_record WHERE task_id = #{taskId} ORDER BY id
    </select>

    <select id="selectByTaskIdAndStatus" resultMap="ChangeRecordResultMap">
        SELECT * FROM change_record
        WHERE task_id = #{taskId} AND status = #{status}
        ORDER BY id
    </select>

    <select id="selectByTaskIdAndOperationType" resultMap="ChangeRecordResultMap">
        SELECT * FROM change_record
        WHERE task_id = #{taskId} AND operation_type = #{operationType}
        ORDER BY id
    </select>

    <select id="selectByTaskIdAndChanged" resultMap="ChangeRecordResultMap">
        SELECT * FROM change_record
        WHERE task_id = #{taskId} AND changed = #{changed}
        ORDER BY id
    </select>

    <select id="selectByPage" resultMap="ChangeRecordResultMap">
        SELECT * FROM change_record
        <where>
            <if test="taskId != null and taskId != ''">
                AND task_id = #{taskId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="changed != null">
                AND changed = #{changed}
            </if>
            <if test="keyword != null and keyword != ''">
                <choose>
                    <when test="searchFields != null and searchFields != ''">
                        AND
                        <trim prefixOverrides="OR">
                            <foreach item="field" collection="searchFields.split(',')">
                                OR ${field} LIKE '%' || #{keyword} || '%'
                            </foreach>
                        </trim>
                    </when>
                    <otherwise>
                        AND (original_name LIKE '%' || #{keyword} || '%'
                             OR new_name LIKE '%' || #{keyword} || '%'
                             OR file_path LIKE '%' || #{keyword} || '%'
                             OR new_path LIKE '%' || #{keyword} || '%')
                    </otherwise>
                </choose>
            </if>
        </where>
        <choose>
            <when test="sortBy != null and sortBy != ''">
                ORDER BY ${sortBy} ${sortOrder}
            </when>
            <otherwise>
                ORDER BY id
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByPage" resultType="int">
        SELECT COUNT(*) FROM change_record
        <where>
            <if test="taskId != null and taskId != ''">
                AND task_id = #{taskId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="changed != null">
                AND changed = #{changed}
            </if>
            <if test="keyword != null and keyword != ''">
                <choose>
                    <when test="searchFields != null and searchFields != ''">
                        AND
                        <trim prefixOverrides="OR">
                            <foreach item="field" collection="searchFields.split(',')">
                                OR ${field} LIKE '%' || #{keyword} || '%'
                            </foreach>
                        </trim>
                    </when>
                    <otherwise>
                        AND (original_name LIKE '%' || #{keyword} || '%'
                             OR new_name LIKE '%' || #{keyword} || '%'
                             OR file_path LIKE '%' || #{keyword} || '%'
                             OR new_path LIKE '%' || #{keyword} || '%')
                    </otherwise>
                </choose>
            </if>
        </where>
    </select>

    <insert id="batchInsert">
        INSERT INTO change_record (
            task_id, record_id, original_name, new_name, file_path, new_path,
            operation_type, status, changed, selected, fail_reason, extra_params,
            analyze_time, execute_time, created_at
        ) VALUES
        <foreach collection="records" item="record" separator=",">
            (
                #{record.taskId}, #{record.recordId}, #{record.originalName}, #{record.newName},
                #{record.filePath}, #{record.newPath}, #{record.operationType}, #{record.status},
                #{record.changed}, #{record.selected}, #{record.failReason}, #{record.extraParams},
                #{record.analyzeTime}, #{record.executeTime}, #{record.createdAt}
            )
        </foreach>
    </insert>

    <update id="updateStatus">
        UPDATE change_record SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateSelected">
        UPDATE change_record SET selected = #{selected} WHERE id = #{id}
    </update>

    <select id="getLastInsertId" resultType="long">
        SELECT last_insert_rowid()
    </select>

</mapper>
