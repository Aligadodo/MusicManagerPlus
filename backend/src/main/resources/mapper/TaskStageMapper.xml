<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.filemanager.backend.mapper.TaskStageMapper">

    <resultMap id="TaskStageResultMap" type="com.filemanager.backend.entity.TaskStagePO">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="stageType" column="stage_type"/>
        <result property="status" column="status"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="duration" column="duration"/>
        <result property="totalFiles" column="total_files"/>
        <result property="processedFiles" column="processed_files"/>
        <result property="successCount" column="success_count"/>
        <result property="failedCount" column="failed_count"/>
        <result property="changedFiles" column="changed_files"/>
        <result property="statsJson" column="stats_json"/>
    </resultMap>

    <insert id="insert" parameterType="com.filemanager.backend.entity.TaskStagePO">
        INSERT INTO task_stage (
            task_id, stage_type, status, start_time, end_time, duration,
            total_files, processed_files, success_count, failed_count, changed_files, stats_json
        ) VALUES (
            #{taskId}, #{stageType}, #{status}, #{startTime}, #{endTime}, #{duration},
            #{totalFiles}, #{processedFiles}, #{successCount}, #{failedCount}, #{changedFiles}, #{statsJson}
        )
    </insert>

    <update id="update" parameterType="com.filemanager.backend.entity.TaskStagePO">
        UPDATE task_stage
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="totalFiles != null">total_files = #{totalFiles},</if>
            <if test="processedFiles != null">processed_files = #{processedFiles},</if>
            <if test="successCount != null">success_count = #{successCount},</if>
            <if test="failedCount != null">failed_count = #{failedCount},</if>
            <if test="changedFiles != null">changed_files = #{changedFiles},</if>
            <if test="statsJson != null">stats_json = #{statsJson},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM task_stage WHERE id = #{id}
    </delete>

    <delete id="deleteByTaskId">
        DELETE FROM task_stage WHERE task_id = #{taskId}
    </delete>

    <select id="selectById" resultMap="TaskStageResultMap">
        SELECT * FROM task_stage WHERE id = #{id}
    </select>

    <select id="selectByTaskId" resultMap="TaskStageResultMap">
        SELECT * FROM task_stage WHERE task_id = #{taskId} ORDER BY id
    </select>

    <select id="selectByTaskIdAndStageType" resultMap="TaskStageResultMap">
        SELECT * FROM task_stage
        WHERE task_id = #{taskId} AND stage_type = #{stageType}
    </select>

    <select id="selectByTaskIdAndStatus" resultMap="TaskStageResultMap">
        SELECT * FROM task_stage
        WHERE task_id = #{taskId} AND status = #{status}
        ORDER BY id
    </select>

    <update id="updateStatus">
        UPDATE task_stage SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateDuration">
        UPDATE task_stage SET duration = #{duration} WHERE id = #{id}
    </update>

</mapper>
