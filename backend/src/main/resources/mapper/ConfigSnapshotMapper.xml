<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.filemanager.backend.mapper.ConfigSnapshotMapper">

    <resultMap id="ConfigSnapshotResultMap" type="com.filemanager.backend.entity.ConfigSnapshotPO">
        <id property="snapshotId" column="snapshot_id"/>
        <result property="snapshotName" column="snapshot_name"/>
        <result property="snapshotType" column="snapshot_type"/>
        <result property="configData" column="config_data"/>
        <result property="description" column="description"/>
        <result property="isTemplate" column="is_template"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="createdBy" column="created_by"/>
    </resultMap>

    <insert id="insert" parameterType="com.filemanager.backend.entity.ConfigSnapshotPO">
        INSERT OR REPLACE INTO config_snapshot (
            snapshot_id, snapshot_name, snapshot_type, config_data, description,
            is_template, created_at, updated_at, created_by
        ) VALUES (
            #{snapshotId}, #{snapshotName}, #{snapshotType}, #{configData}, #{description},
            #{isTemplate}, #{createdAt}, #{updatedAt}, #{createdBy}
        )
    </insert>

    <update id="update" parameterType="com.filemanager.backend.entity.ConfigSnapshotPO">
        UPDATE config_snapshot
        <set>
            <if test="snapshotName != null">snapshot_name = #{snapshotName},</if>
            <if test="snapshotType != null">snapshot_type = #{snapshotType},</if>
            <if test="configData != null">config_data = #{configData},</if>
            <if test="description != null">description = #{description},</if>
            <if test="isTemplate != null">is_template = #{isTemplate},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE snapshot_id = #{snapshotId}
    </update>

    <delete id="deleteBySnapshotId">
        DELETE FROM config_snapshot WHERE snapshot_id = #{snapshotId}
    </delete>

    <delete id="deleteById">
        DELETE FROM config_snapshot WHERE snapshot_id = #{snapshotId}
    </delete>

    <delete id="deleteOldSnapshots">
        DELETE FROM config_snapshot WHERE created_at &lt; #{cutoffDate}
    </delete>

    <select id="selectBySnapshotId" resultMap="ConfigSnapshotResultMap">
        SELECT * FROM config_snapshot WHERE snapshot_id = #{snapshotId}
    </select>

    <select id="selectById" resultMap="ConfigSnapshotResultMap">
        SELECT * FROM config_snapshot WHERE snapshot_id = #{snapshotId}
    </select>

    <select id="selectAll" resultMap="ConfigSnapshotResultMap">
        SELECT * FROM config_snapshot ORDER BY created_at DESC
    </select>

    <select id="selectBySnapshotType" resultMap="ConfigSnapshotResultMap">
        SELECT * FROM config_snapshot
        WHERE snapshot_type = #{snapshotType}
        ORDER BY created_at DESC
    </select>

    <select id="selectByType" resultMap="ConfigSnapshotResultMap">
        SELECT * FROM config_snapshot
        WHERE snapshot_type = #{snapshotType}
        ORDER BY created_at DESC
    </select>

    <select id="getLatestSnapshotByType" resultMap="ConfigSnapshotResultMap">
        SELECT * FROM config_snapshot
        WHERE snapshot_type = #{snapshotType}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="selectByIsTemplate" resultMap="ConfigSnapshotResultMap">
        SELECT * FROM config_snapshot
        WHERE is_template = #{isTemplate}
        ORDER BY created_at DESC
    </select>

    <select id="selectByPage" resultMap="ConfigSnapshotResultMap">
        SELECT * FROM config_snapshot
        <where>
            <if test="snapshotType != null and snapshotType != ''">
                AND snapshot_type = #{snapshotType}
            </if>
            <if test="isTemplate != null">
                AND is_template = #{isTemplate}
            </if>
            <if test="startDate != null">
                AND created_at &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND created_at &lt;= #{endDate}
            </if>
        </where>
        <choose>
            <when test="sortBy != null and sortBy != ''">
                ORDER BY ${sortBy} ${sortOrder}
            </when>
            <otherwise>
                ORDER BY created_at DESC
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByPage" resultType="int">
        SELECT COUNT(*) FROM config_snapshot
        <where>
            <if test="snapshotType != null and snapshotType != ''">
                AND snapshot_type = #{snapshotType}
            </if>
            <if test="isTemplate != null">
                AND is_template = #{isTemplate}
            </if>
            <if test="startDate != null">
                AND created_at &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND created_at &lt;= #{endDate}
            </if>
        </where>
    </select>

    <update id="updateConfigData">
        UPDATE config_snapshot
        SET config_data = #{configData}, updated_at = datetime('now')
        WHERE snapshot_id = #{snapshotId}
    </update>

</mapper>
