<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.filemanager.backend.mapper.ConfigTemplateMapper">

    <resultMap id="ConfigTemplateResultMap" type="com.filemanager.backend.entity.ConfigTemplatePO">
        <id property="templateId" column="template_id"/>
        <result property="templateName" column="template_name"/>
        <result property="templateType" column="template_type"/>
        <result property="snapshotId" column="snapshot_id"/>
        <result property="category" column="category"/>
        <result property="tags" column="tags"/>
        <result property="description" column="description"/>
        <result property="isDefault" column="is_default"/>
        <result property="usageCount" column="usage_count"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="createdBy" column="created_by"/>
    </resultMap>

    <insert id="insert" parameterType="com.filemanager.backend.entity.ConfigTemplatePO">
        INSERT INTO config_template (
            template_id, template_name, template_type, snapshot_id, category,
            tags, description, is_default, usage_count, created_at, updated_at, created_by
        ) VALUES (
            #{templateId}, #{templateName}, #{templateType}, #{snapshotId}, #{category},
            #{tags}, #{description}, #{isDefault}, #{usageCount}, #{createdAt}, #{updatedAt}, #{createdBy}
        )
    </insert>

    <update id="update" parameterType="com.filemanager.backend.entity.ConfigTemplatePO">
        UPDATE config_template
        <set>
            <if test="templateName != null">template_name = #{templateName},</if>
            <if test="templateType != null">template_type = #{templateType},</if>
            <if test="snapshotId != null">snapshot_id = #{snapshotId},</if>
            <if test="category != null">category = #{category},</if>
            <if test="tags != null">tags = #{tags},</if>
            <if test="description != null">description = #{description},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE template_id = #{templateId}
    </update>

    <delete id="deleteByTemplateId">
        DELETE FROM config_template WHERE template_id = #{templateId}
    </delete>

    <select id="selectByTemplateId" resultMap="ConfigTemplateResultMap">
        SELECT * FROM config_template WHERE template_id = #{templateId}
    </select>

    <select id="selectAll" resultMap="ConfigTemplateResultMap">
        SELECT * FROM config_template ORDER BY created_at DESC
    </select>

    <select id="selectByTemplateType" resultMap="ConfigTemplateResultMap">
        SELECT * FROM config_template
        WHERE template_type = #{templateType}
        ORDER BY created_at DESC
    </select>

    <select id="selectByCategory" resultMap="ConfigTemplateResultMap">
        SELECT * FROM config_template
        WHERE category = #{category}
        ORDER BY created_at DESC
    </select>

    <select id="selectByIsDefault" resultMap="ConfigTemplateResultMap">
        SELECT * FROM config_template
        WHERE is_default = #{isDefault}
        ORDER BY created_at DESC
    </select>

    <select id="selectByPage" resultMap="ConfigTemplateResultMap">
        SELECT * FROM config_template
        <where>
            <if test="templateType != null and templateType != ''">
                AND template_type = #{templateType}
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="isDefault != null">
                AND is_default = #{isDefault}
            </if>
        </where>
        <choose>
            <when test="sortBy != null and sortBy != ''">
                ORDER BY ${sortBy} ${sortOrder}
            </when>
            <otherwise>
                ORDER BY created_at DESC
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByPage" resultType="int">
        SELECT COUNT(*) FROM config_template
        <where>
            <if test="templateType != null and templateType != ''">
                AND template_type = #{templateType}
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="isDefault != null">
                AND is_default = #{isDefault}
            </if>
        </where>
    </select>

    <update id="incrementUsageCount">
        UPDATE config_template
        SET usage_count = usage_count + 1, updated_at = datetime('now')
        WHERE template_id = #{templateId}
    </update>

</mapper>
